"""Schedule-reconciliation capability package.

Import the explicit ``api`` and ``contracts`` modules; the package root is
intentionally non-aggregating so importing the package does not pull in every
implementation file.
"""
