# `xcron jobs`

Inspect and edit jobs inside one selected schedule manifest.

These commands edit YAML only; use `xcron apply` to reconcile backend state after manifest changes.

Use the group when you need manifest-side job lifecycle commands such as `list`, `show`, `add`, `update`, `enable`, `disable`, or `remove`.
