# `xcron jobs show`

Show one manifest job by local or qualified id without touching the scheduler backend.

Use this for YAML-side inspection when you do not need deployed backend detail from `xcron inspect`.
