# `xcron plan`

Show the scheduler changes xcron would make for the selected manifest using local derived state.

This command is backend-aware but does not inspect the live scheduler. Use `status` when you need ground truth from deployed backend state.
