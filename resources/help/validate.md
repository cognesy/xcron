# `xcron validate`

Validate the selected manifest under `resources/schedules/` without mutating scheduler state.

Use this command first when you need to confirm manifest shape, semantic validity, job count, and stable hashes before planning or applying changes.
