# Services

Services provide reusable capabilities that are independent of the CLI shell.

They will eventually encapsulate configuration loading, schema validation,
wrapper generation, backend integrations, hashing, state storage, and related
filesystem and process operations.
