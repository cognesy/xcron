[package]
kind = "provider"
capability = "metrics"
implementation = "test"
namespace = "xcron.capabilities.metrics_test"
kernel_api = ">=1,<2"

[provider]
entry_point = "xcron.capabilities.metrics_test.provider:CAPABILITY"
ports = ["metrics"]
requires = []
cli_paths = []
assets = []

[operations]
justfile = "justfile"
required_recipes = ["default", "list", "doctor", "check", "test", "build"]
