[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "xcron-capability-metrics-local"
version = "0.1.4"
description = "Best-effort local runtime metrics provider for xcron"
readme = "README.md"
requires-python = ">=3.11"
license = "LicenseRef-Proprietary"
dependencies = [
  "xcron-contracts==0.1.4",
  "xcron-kernel==0.1.4",
]

[project.entry-points."xcron.capabilities"]
metrics-local = "xcron.capabilities.metrics_local.provider:CAPABILITY"

[tool.uv.sources]
xcron-contracts = { workspace = true }
xcron-kernel = { workspace = true }

[tool.setuptools]
packages = ["xcron.capabilities.metrics_local"]

[tool.setuptools.package-dir]
"" = "src"
