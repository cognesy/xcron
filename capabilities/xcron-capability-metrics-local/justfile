set positional-arguments := true

ROOT := parent_directory(parent_directory(justfile_directory()))

default:
    @just --list

list:
    @just --list

doctor:
    @uv run --project {{ROOT}} python -c "from xcron.capabilities.metrics_local.provider import CAPABILITY; assert CAPABILITY.id == 'metrics:local'"

check:
    @uv run --project {{ROOT}} pytest {{ROOT}}/capabilities/xcron-capability-metrics-local/tests -q

test:
    @uv run --project {{ROOT}} pytest {{ROOT}}/capabilities/xcron-capability-metrics-local/tests -q

build:
    @uv build --project {{ROOT}} --package xcron-capability-metrics-local --out-dir /tmp/xcron-metrics-local-build --clear
