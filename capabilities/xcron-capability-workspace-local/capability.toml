[package]
kind = "provider"
capability = "workspace"
implementation = "local"
namespace = "xcron.capabilities.workspace_local"
kernel_api = ">=1,<2"

[provider]
entry_point = "xcron.capabilities.workspace_local.provider:CAPABILITY"
ports = ["workspace"]
requires = []
cli_paths = ["init"]
assets = ["resources/starter-manifest.yaml"]

[operations]
justfile = "justfile"
required_recipes = ["default", "list", "doctor", "check", "test", "build"]
