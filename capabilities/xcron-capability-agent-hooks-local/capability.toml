[package]
kind = "provider"
capability = "agent-hooks"
implementation = "local"
namespace = "xcron.capabilities.agent_hooks_local"
kernel_api = ">=1,<2"

[provider]
entry_point = "xcron.capabilities.agent_hooks_local.provider:CAPABILITY"
ports = ["agent-hooks"]
requires = []
cli_paths = ["hooks"]
assets = []

[operations]
justfile = "justfile"
required_recipes = ["default", "list", "doctor", "check", "test", "build"]
